<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
<link rel="stylesheet" href="/css/searchbar.css">
</head>
<body>
    <%- include('partials/header.ejs') %>
<div class="box">

  <h1>Find a ride</h1>
    <%- include('partials/searchbar.ejs') %>
 
  <h1>Travel for these popular routes</h1>
    <div id="allrides">
      <ul class="ride-list">
      <% rides.filter(ride => ride.publisherId!== userId)
        .forEach(ride => { %>
        <li class="ride-item">
         <a href="/route-rides?from=<%= ride.location %>&to=<%= ride.destination %>" style="text-decoration:none; color:inherit;">

            <strong><%= ride.location %> => <%= ride.destination %></strong><br>
            <p><%= ride.seats %> Passengers</p>
          </a>
          <p id="cost"><%= ride.price %></p>
        </li>
      <% }); %>
    </ul>
    </div>
</div>

</body>
</html>